use crate::vulkan::instance::VulkanInstanceManager;

pub struct PhysicalDeviceManager {}

impl PhysicalDeviceManager {
    pub fn new(instance_manager: &VulkanInstanceManager) -> Self {
        Self {}
    }
}
